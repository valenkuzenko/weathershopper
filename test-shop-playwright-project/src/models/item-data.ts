export class ItemData {
    public itemTitle: string;
    public itemPrice: number;

    constructor(itemTitle: string, itemPrice: number) {
        this.itemTitle = itemTitle;
        this.itemPrice = itemPrice;
    }
}